## First load required libraries 
library(openintro)
library(ggplot2)

# Question 1. 
# (1) Make a scatter plot of the variables `pop2017` (for x) and `pop2000` (for y) in `loan50`.
# (2) What kind of associations do they have?

# Question 2. 
# (1) Make a histogram of the variable `interest_rate` in `loan50`.
# (2) Is it symmeric, skewed-left, skewed-right?

# Question 3. Consider the variable `` in `loan50`.
# (1) Save the sorted loan50@ and to the variable my_var.
my_var <- sort(<>)

# (2) Find Q1, median, and Q3 
median(my_var)

# (3) Make a boxplot


# Question 4. Save your work as 9-10-r_worksheet_your_name.R ans upload it Canvas: 
# https://csusb.instructure.com/courses/42324/assignments/670837